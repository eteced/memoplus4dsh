# M16 — 泛化性验证（全新题集 + 跨长度）

> 日期：2026-09-08 · 目的：回答"之前的迭代是不是对固定题集过拟合了"
> 集合：**第二 mini 集**（`MINI_OFFSET=1` + `MINI_LENGTHS="6k 32k"`）——题目与此前全部 7 轮迭代零重叠，并加回 32k 档验证跨长度泛化。tag=m16v1。

## 结果

| 档位 | 分数 | 注入召回率 |
|---|---|---|
| sh_6k | **100%** | 100% |
| sh_32k | 60% | 100% |
| mh_6k | 60% | 100% |
| mh_32k | 60% | 80% |
| LME（judge） | **80%** | multi-session 0.75（原最弱分项） |

- **注入召回率全 CR 95%**（run-2 基线 7.7%）；纯检索失败 **0**；
- 6 个失败中 5 个是"已注入但模型答错"（模型侧），1 个未搜索——记忆系统侧无新增归因失败；
- 审计全 PASS；题集与迭代集零重叠。

## 结论

方法**泛化成立**：在从未见过的题目和 32k 长度上，记忆系统侧的召回指标与迭代集持平甚至更好（95% vs 88%）。分数侧的差异全部是模型侧（已注入但答错），不是记忆召回问题。

注意：LME 的 judge 分数跨题集不严格可比（stride 不同选题不同）；CR 分数在 n=5 上的波动是噪声，看注入召回率和归因分布更可靠。
