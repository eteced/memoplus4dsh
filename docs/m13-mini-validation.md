# M13 — mini 集合验证（记忆插件性能评估）

> 日期：2026-09-05 · 构建：含全部 M11 修复 + M12 NER 辅助 + 存储三项修复 + 合并加固
> 集合：mini（CR sh/mh 6k 各 5 题 + LME 1 context 5 题，`MINI_TAG=m13v1`）
> 重点（用户要求）：评估记忆插件本身的性能，而非模型能力。

## 1. 记忆系统指标（CR 段，10 题）

| 指标 | m13v1 | run-2 基线（v0） |
|---|---|---|
| 注入召回率（SH） | **100%** | 8.0% |
| 注入召回率（MH） | 40% | 0% |
| 最终召回率（SH/MH） | 100% / 60% | ~12% / ~60% |
| 纯检索失败（搜过仍 miss） | **0** | 167 |
| 已注入但答错（模型侧） | 1 | 9 |
| 重名实体组 | 0 | 2820 |
| 审计（非记忆工具执行） | 0 次 | 0 次 |

**答对率**：sh_6k 80%（4/5，唯一失败为模型侧）、mh_6k **60%（3/5，历史最好）**。
MH 剩余 2 题为深链题：模型未主动逐跳搜索（模型侧策略问题），其中 1 题图归因弱证据（提取的事实表述形式不同）。

## 2. 性能（速度/成本）

| 指标 | m13v1 | 备注 |
|---|---|---|
| CR ingest 墙钟 | **107–111s/context** | 较 m11v7/m12v2（126–151s）更快——存储修复净收益超过裁决开销 |
| 单题查询耗时 | sh avg 17s / mh avg 34s | mh 难题上模型多轮 memory_search（侦探模式尾部延迟，已知） |
| LME ingest | ~50min（串行抽取 + 裁决） | drain 超时曾致中止（1800s 预算不足），已提额至 7200s |

## 3. LME 段

- **judge 100%（5/5）**，temporal-reasoning 2/2（历史轨迹：mini-1 60% → mini-5 80% → m13v1 100%；子集题组不同仅作方向参考）。
- 注入召回率 **100%**（4/4 可判定）。
- 审计 PASS（0 次非记忆工具执行、0 可疑内容）。

## 5. 全 mini 归因汇总（15 题）

| 指标 | m13v1 |
|---|---|
| 注入召回率 | **78.6%**（SH 100% / LME 100% / MH 40%） |
| 最终召回率 | 85.7% |
| 纯检索失败 | **0** |
| 纯写入失败（图中无答案） | 1（弱证据，非确认） |
| 模型侧（已召回但答错） | 1 |

## 4. 本轮新发现的基建问题

- `wait_queue_drain` 1800s 预算在裁决开销下的 LME context 不够（串行抽取 ~50min）→ 中止。已提额 7200s（`agent_memoplus_dsh.py`）。
- 个别抽取轮次仍出现 "extraction produced empty content"（F-1 模式尾部），按有界重试跳过，未再阻塞队列。
